<header class="d-flex justify-content-between container mx-auto py-3 mt-2">
    <a href="{{route('index')}}">
        <h3 class="fw-bold">Amazing<span class="text-primary">News</span></h3>
    </a>
    <a href="{{route('login')}}" class="btn btn-primary text-light">MASUK</a>
</header>